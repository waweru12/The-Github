import { LighterDirective } from './lighter.directive';

describe('LighterDirective', () => {
  it('should create an instance', () => {
    const directive = new LighterDirective();
    expect(directive).toBeTruthy();
  });
});
